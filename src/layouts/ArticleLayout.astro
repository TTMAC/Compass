---
import BaseLayout from "./BaseLayout.astro";
import TableOfContents from "@components/TableOfContents.astro";
import { getPillarBySlug } from "@content/pillars";

interface Heading {
  depth: number;
  text: string;
  slug: string;
}

interface Props {
  title: string;
  subtitle: string;
  description: string;
  articleNumber: string;
  pillar?: string;
  part?: number;
  sphere?: ("national" | "provincial" | "municipal" | "all")[];
  publishDate: Date;
  readingTime: number;
  canonicalUrl?: string;
  ogImage?: string;
  series: {
    prev: string | null;
    next: string | null;
  };
  headings?: Heading[];
}

const {
  title,
  subtitle,
  description,
  articleNumber,
  pillar = "government-structure",
  part,
  sphere,
  publishDate,
  readingTime,
  canonicalUrl,
  ogImage,
  headings = [],
} = Astro.props;

const pillarConfig = getPillarBySlug(pillar);
const partConfig = pillarConfig?.parts.find((p) => p.number === part);

const sphereColors: Record<string, string> = {
  national: "bg-sphere-national",
  provincial: "bg-sphere-provincial",
  municipal: "bg-sphere-municipal",
  all: "bg-compass-green",
};

const formattedDate = publishDate.toLocaleDateString("en-ZA", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  headline: title,
  description: description,
  datePublished: publishDate.toISOString(),
  author: {
    "@type": "Organization",
    name: "GovCompass",
    url: Astro.site?.origin || "https://govcompass.co.za",
  },
  publisher: {
    "@type": "Organization",
    name: "GovCompass",
    url: Astro.site?.origin || "https://govcompass.co.za",
  },
};
---

<BaseLayout
  title={`${title} — GovCompass`}
  description={description}
  canonicalUrl={canonicalUrl}
  ogImage={ogImage}
  ogType="article"
  jsonLd={jsonLd}
>
  <div class="article-grid">
    <article class="article-main px-4 md:px-0 py-8 md:py-12">
      {pillarConfig && (
        <nav class="flex items-center gap-1.5 text-xs font-heading text-text-secondary mb-4" aria-label="Breadcrumb">
          <a href={`/pillars/${pillar}`} class="hover:text-compass-green transition-colors" style={`color: ${pillarConfig.color}`}>
            {pillarConfig.shortTitle}
          </a>
          {partConfig && (
            <>
              <span aria-hidden="true">&rsaquo;</span>
              <span>Part {part}: {partConfig.title}</span>
            </>
          )}
          <span aria-hidden="true">&rsaquo;</span>
          <span>Article {articleNumber}</span>
        </nav>
      )}

      <header class="mb-8">
        <div class="flex items-center gap-3 mb-4">
          <span class="font-heading text-sm text-text-secondary">
            Article {articleNumber}
          </span>
          {sphere?.map((s) => (
            <span
              class={`inline-block px-2 py-0.5 text-xs font-heading font-semibold text-white rounded ${sphereColors[s]}`}
            >
              {s.charAt(0).toUpperCase() + s.slice(1)}
            </span>
          ))}
        </div>

        <h1 class="font-heading text-3xl md:text-4xl font-bold text-text-primary mb-3 leading-tight">
          {title}
        </h1>

        <p class="text-lg md:text-xl text-text-secondary mb-4 font-body">
          {subtitle}
        </p>

        <div class="flex items-center gap-4 text-sm text-text-secondary font-heading">
          <time datetime={publishDate.toISOString()}>
            {formattedDate}
          </time>
          <span aria-label={`${readingTime} minute read`}>
            {readingTime} min read
          </span>
        </div>

      </header>

      {headings.length > 0 && (
        <div class="toc-mobile-wrapper">
          <TableOfContents headings={headings} />
        </div>
      )}

      <div class="prose prose-article" data-testid="article-content">
        <slot />
      </div>
    </article>

    {headings.length > 0 && (
      <aside class="toc-sidebar">
        <TableOfContents headings={headings} />
      </aside>
    )}
  </div>
</BaseLayout>

<style>
  .article-grid {
    display: grid;
    grid-template-columns: 1fr;
    max-width: 680px;
    margin: 0 auto;
  }

  @media (min-width: 1100px) {
    .article-grid {
      grid-template-columns: 680px 240px;
      max-width: none;
      width: fit-content;
      margin: 0 auto;
      gap: 2rem;
    }
  }

  .article-main {
    max-width: 680px;
    min-width: 0;
  }

  .toc-mobile-wrapper {
    display: block;
  }

  .toc-sidebar {
    display: none;
  }

  @media (min-width: 1100px) {
    .toc-mobile-wrapper {
      display: none;
    }

    .toc-sidebar {
      display: block;
      position: relative;
      padding-top: 8rem;
    }
  }

  .prose-article {
    font-size: 19px;
    line-height: 1.65;
  }

  @media (max-width: 768px) {
    .prose-article {
      font-size: 17px;
    }
  }

  .prose-article :global(h2) {
    font-family: "Inter", system-ui, sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
    color: #1a1a1a;
  }

  .prose-article :global(h3) {
    font-family: "Inter", system-ui, sans-serif;
    font-size: 1.25rem;
    font-weight: 600;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
    color: #1a1a1a;
  }

  .prose-article :global(p) {
    margin-bottom: 1.25rem;
  }

  .prose-article :global(strong) {
    font-weight: 700;
  }

  .prose-article :global(a) {
    color: #1b6b4a;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .prose-article :global(a:hover) {
    color: #145236;
  }

  .prose-article :global(ul),
  .prose-article :global(ol) {
    margin-bottom: 1.25rem;
    padding-left: 1.5rem;
  }

  .prose-article :global(li) {
    margin-bottom: 0.5rem;
  }

  .prose-article :global(blockquote) {
    border-left: 4px solid #1b6b4a;
    padding-left: 1rem;
    margin: 1.5rem 0;
    color: #4a4a4a;
    font-style: italic;
  }

  /* Hero image */
  .hero-image {
    margin: 0;
    border-radius: 0.5rem;
    overflow: hidden;
  }

  .hero-image img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 0.5rem;
  }

  @media (max-width: 768px) {
    .hero-image {
      margin-left: -1rem;
      margin-right: -1rem;
      border-radius: 0;
    }

    .hero-image img {
      border-radius: 0;
    }
  }

  /* Article images */
  .prose-article :global(figure.article-figure) {
    margin: 2rem 0;
    border-radius: 0.5rem;
    overflow: hidden;
  }

  @media (max-width: 768px) {
    .prose-article :global(figure.article-figure) {
      margin-left: -0.5rem;
      margin-right: -0.5rem;
      border-radius: 0;
    }
  }

  .prose-article :global(figure.article-figure img) {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 0.5rem 0.5rem 0 0;
  }

  @media (max-width: 768px) {
    .prose-article :global(figure.article-figure img) {
      border-radius: 0;
    }
  }

  .prose-article :global(figcaption.article-figcaption) {
    font-family: "Inter", system-ui, sans-serif;
    font-size: 0.8125rem;
    line-height: 1.4;
    color: #6b6b6b;
    padding: 0.5rem 0;
  }

  .prose-article :global(.article-caption-text) {
    color: #4a4a4a;
  }

  .prose-article :global(.article-attribution) {
    font-size: 0.75rem;
    color: #9a9a9a;
  }

  .prose-article :global(.article-attribution a) {
    color: #9a9a9a;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .prose-article :global(.article-attribution a:hover) {
    color: #6b6b6b;
  }
</style>

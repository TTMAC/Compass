---
import PageLayout from "@layouts/PageLayout.astro";
---

<PageLayout
  title="Subscribe"
  description="Get new Compass articles delivered to your inbox. Free political education for South Africa, no spam."
>
  <div class="max-w-md mx-auto text-center">
    <h1 class="font-heading text-3xl font-bold text-text-primary mb-4">
      Subscribe
    </h1>

    <p class="text-text-secondary mb-8 leading-relaxed">
      Get notified when new articles are published. No spam, no third-party
      sharing — just political education delivered to your inbox.
    </p>

    <form
      name="subscribe"
      method="POST"
      data-netlify="true"
      netlify-honeypot="bot-field"
      class="space-y-4"
      data-testid="subscribe-form"
    >
      <input type="hidden" name="form-name" value="subscribe" />
      <p class="hidden">
        <label>
          Don't fill this out: <input name="bot-field" />
        </label>
      </p>

      <div>
        <label for="email" class="sr-only">Email address</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          placeholder="your@email.com"
          class="w-full px-4 py-3 border border-gray-300 rounded font-body text-text-primary placeholder:text-gray-400 focus:outline-none focus:ring-2 focus:ring-compass-green focus:border-transparent"
        />
      </div>

      <button
        type="submit"
        class="w-full px-6 py-3 bg-compass-green text-white font-heading font-semibold rounded hover:bg-opacity-90 transition-colors"
      >
        Subscribe
      </button>
    </form>

    <p class="mt-6 text-xs text-text-secondary">
      We respect your privacy. Read our
      <a href="/privacy" class="text-compass-green underline">privacy policy</a
      >.
    </p>
  </div>
</PageLayout>

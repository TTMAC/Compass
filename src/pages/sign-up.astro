---
import { SignUp } from "@clerk/astro/components";

// Render on-demand so the Clerk middleware runs for the sign-up handshake.
export const prerender = false;
---

<div class="flex min-h-screen items-center justify-center">
  <SignUp />
</div>
